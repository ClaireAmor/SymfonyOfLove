<header class="px-container mb-[115px]">
	<div class="flex justify-between items-center">
		<div class="w-8/12">
			<a href="{{ path('app_login') }}" class="text-60 font-serif tracking-[1.25rem] text-green">Symfony Of Love</a>
		</div>
		<div class="flex gap-x-[30px] font-sans text-30">
            <a href="{{ path('app_profile') }}">plants</a>
            <a href="{{ path('app_profile') }}">profile</a>
            <a href="#">my reviews</a>

			{% if is_granted('ROLE_ADMIN') %} 
				<a href="#">admin</a>
			{% endif %}
        </div>
	</div>
</header>