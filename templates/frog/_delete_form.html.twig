<form method="post" action="{{ path('app_frog_delete', {'id': frog.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ frog.id) }}">
    <button class="btn">Delete</button>
</form>
